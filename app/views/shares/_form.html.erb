<%= form_for([@case_log, @share]) do |f| %>
  <% if @share.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@share.errors.count, "error") %> prohibited this share from being saved:</h2>

      <ul>
      <% @share.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label "Enter the email of the doctor that you want to share this log with:" %><br>
    <%= f.text_field :doctor_email %>
  </div>

  <div class="actions">
    <%= f.submit "Share" %>
  </div>
<% end %>

